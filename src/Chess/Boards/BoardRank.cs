namespace Chess;

public union BoardRank(One, Two, Three, Four, Five, Six, Seven, Eight);

public sealed record One;

public sealed record Two;

public sealed record Three;

public sealed record Four;

public sealed record Five;

public sealed record Six;

public sealed record Seven;

public sealed record Eight;
